"""
main.py

Ponto de entrada da API do catálogo automotivo.

Objetivo futuro:
- expor endpoints HTTP para consulta do catálogo
- permitir busca por código, peça, motor e veículo
- servir como camada de entrega do sistema

Exemplos de endpoints futuros:
- GET /search/code/{code}
- GET /search/part
- GET /search/vehicle
- GET /fitment/vehicle/{vehicle_id}

Status atual:
- arquivo reservado para implementação futura
"""